import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    common: { black: '#1b1f23' },
    mode: 'dark',
    primary: { dark: '#24292e', main: '#2f363d' },
    text: { primary: '#e1e4e8' },
  },
});
