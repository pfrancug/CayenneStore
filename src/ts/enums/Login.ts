export enum Login {
  Anonymous = 'Anonymous',
  DatabaseConnection = 'Connected to database as:',
  DatabaseNoConnection = 'No database connection.',
  Login = 'Login',
  Password = 'Password',
  SignIn = 'Sign In',
  SignOut = 'Sign Out',
}
